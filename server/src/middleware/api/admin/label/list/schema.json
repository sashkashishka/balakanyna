{
  "title": "LabelListSearchParams",
  "type": "object",
  "properties": {
    "order_by": {
      "type": "string",
      "enum": [
        "createdAt",
        "updatedAt",
        "name"
      ]
    },
    "dir": {
      "type": "string",
      "enum": [
        "asc",
        "desc"
      ]
    },
    "type": {
      "type": "string",
      "enum": [
        "image",
        "task"
      ]
    },
    "name": {
      "type": "string"
    }
  },
  "required": [
    "order_by",
    "dir"
  ]
}
